{
  "name": "@healthspan/runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hosted-reachable service layer. Depends on the async ports, never on a driver.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@healthspan/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
