{
  "name": "@healthspan/sites",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Edge-safe Sites runtime. No Node server, no native driver, no filesystem.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@healthspan/core": "workspace:*",
    "@healthspan/db": "workspace:*",
    "@healthspan/runtime": "workspace:*",
    "hono": "^4.12.31"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
